import 'package:flutter/material.dart';

class PopularCategoryDisplayScreen extends StatelessWidget {
  const PopularCategoryDisplayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [],
    );
  }
}
